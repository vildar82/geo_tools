(defun convert-rad-azimuth (rad /)
  ;перевод угла в радианах (0 на восток увеличение против часовой) в дирекционный угол (0 на север увеличение по часовой)
  (if (< (/ pi 2.0) rad)
    (convert-rad-grad(-(* pi 2.0)(- rad (/ pi 2.0))))
    (convert-rad-grad(- (/ pi 2.0) rad))
    )
  )